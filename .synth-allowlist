# Files allowed to contain real-looking synthetic secrets/PII.
#
# These files are deliberately full of cryptographic-looking-but-synthetic
# strings used as bench fixtures. The strings were generated by deterministic
# synthetic-data pipelines (see SOURCES.md and the per-bench MODEL_CARD docs);
# no real credentials or PII were used.
#
# Format: one path per line, relative to repo root. Lines starting with `#` and
# blank lines are ignored. Globs are NOT expanded; list files explicitly so
# every entry is auditable.

# Image bench corpus — generated by `screenpipe-pii-bench-image/training/gen_specs.py`
# from synthetic name/company/host/secret pools. See image/docs/STATUS.md.
image/corpus/annotations.jsonl
image/corpus/annotations_002.jsonl
image/corpus/annotations_train.jsonl
image/corpus/annotations_val.jsonl
image/corpus/annotations_val_50.jsonl
image/corpus/sample/annotations_sample.jsonl

# Text bench corpus + 36-row sample — synthetic.
text/data/sample.jsonl

# Trace bench sample — synthetic injected PII tokens.
trace/data/injected_sample.jsonl

# Image bench HTML templates — synthetic by construction.
image/templates/terminal_session.html

# Text bench secret-detection probe — bench fixture testing secret recognition.
# Strings here are visually-synthetic (`abc123def456...`, `EXAMPLE`, `hunter2`).
text/src/probes/secret_probe.py
